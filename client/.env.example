# Private key for the BUYER wallet (separate from the seller's
# X402_PAY_TO_ADDRESS in the main project's .env — this one spends money,
# that one receives it). Generate one with `npm run generate-wallet`.
EVM_PRIVATE_KEY=

# Where the seller (ai-agent-hub backend) is running.
RESOURCE_SERVER_URL=http://localhost:4021
